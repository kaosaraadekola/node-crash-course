const fs = require ('fs')

//reading files - takes in 2 arguments
fs.readFile()

//writing files - takes in 3 arguments

//directories - need to specify where to make the directories 

//deleting files -